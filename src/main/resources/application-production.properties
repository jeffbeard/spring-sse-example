# Production overrides. The container image no longer ships spring-boot-devtools
# (issue #17); these keep it inert if it ever reaches the classpath by another route.
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false
spring.devtools.add-properties=false
