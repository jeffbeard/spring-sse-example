apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spring-sse-minikube

resources:
  - namespace.yaml
  - ../../base
  - ingress.yaml

patches:
  - path: deployment-patch.yaml
  - path: configmap-patch.yaml

images:
  - name: spring-sse-example
    newName: com.example/spring-sse-example
    newTag: "1.0.0"